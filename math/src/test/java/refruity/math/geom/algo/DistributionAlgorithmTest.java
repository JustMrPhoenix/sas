package refruity.math.geom.algo;

import java.util.Random;

public class DistributionAlgorithmTest {
    protected static final int ITERATIONS = 1000;

    protected static final long SEED = 42;
    protected static final Random RANDOM_GENERATOR = new Random(SEED);
}
